---
name: 01 Azure Landing Zones Continuous Integration

on:
  pull_request:
    branches:
      - main
    paths:
      - '**/*.tf'
      - '!**/.github/**'
  workflow_dispatch:
    inputs:
      terraform_cli_version:
        description: 'Terraform CLI Version'
        required: true
        default: 'latest'
        type: string

jobs:
  discover:
    uses: gregc-dev-01/alz-mgmt/.github/workflows/_discover-modules.yaml@main

  validate_and_plan:
    name: CI • ${{ matrix.module }}
    needs: discover
    if: ${{ needs.discover.outputs.count != '0' }} # avoid empty-matrix error
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.discover.outputs.matrix) }}
    permissions:
      id-token: write
      contents: read
      pull-requests: write
    uses: gregc-dev-01/alz-mgmt/.github/workflows/ci-template.yaml@main
    with:
      root_module_folder_relative_path: ${{ matrix.module }}
      terraform_cli_version: ${{ github.event_name == 'workflow_dispatch' && inputs.terraform_cli_version || 'latest' }}
